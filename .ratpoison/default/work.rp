#!/bin/bash

ratpoison -c "fselect 0"
ratpoison -c "select -"
ratpoison -c "fselect 1"
ratpoison -c "select -"
ratpoison -c "fselect 3"
ratpoison -c "select -"

ratpoison -c "fselect 0"
ratpoison -c "select vim"
ratpoison -c "fselect 3"
ratpoison -c "select term-left"
ratpoison -c "fselect 1"
ratpoison -c "select term-right"

ratpoison -c "fselect 0"

